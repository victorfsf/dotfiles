- name: Gather Facts
  gather_facts: true
  hosts: local

- name: Setup dotfiles
  gather_facts: false
  hosts: local
  user: '{{ ansible_user_id }}'
  become: true
  become_user: root
  become_method: sudo
  roles:
    - base
    - git
    - interface
    - zsh
    - zenv
    - fonts
    - tmux
    - termite
    - fzf
    - nvm
    - pyenv
    - vscode
    - docker
    - redis
    - psql
    - yandex
    - brave
    - chrome
    - spotify
    - vlc
    - heroku
    - transmission
    - gimp
    - libreoffice
    - wallpapers
    - numix-icons
    - adapta-theme
    - finish
